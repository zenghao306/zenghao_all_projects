package model

/*
import (
	"sort"
	"github.com/liudng/godump"
)

func CheckSame(s []int, k int)  {
	if s[k]==s[k-1] {

	}
}
func sortss()  {
	intList := [] int {2, 4, 3, 5, 7, 6, 9, 8, 1, 1}
	sort.Sort(sort.Reverse(sort.IntSlice(intList)))
	for k,v:=range intList {
		godump.Dump(v)


		if k>=1 {
			if intList[k]==intList[k-1] {
				if k>=2 {
					if intList[k-1]==intList[k-2]{
						if k>=3 {
							if intList[k-1]==intList[k-2]{
								intList[]
							}
						}
					}
				}

			}
		}

	}
}

*/
